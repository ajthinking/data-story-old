import { DefaultNodeModel } from "@projectstorm/react-diagrams"

export default class ManipulatorNodeModel extends DefaultNodeModel {
    constructor(name, color)
    {
        super(name, color)
    }
}